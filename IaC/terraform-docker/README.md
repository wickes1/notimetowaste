# Terraform Docker

```bash
terraform init
terraform fmt
terraform validate
terraform plan
terraform apply
# terraform apply -var "container_name=YetAnotherName"
# terraform apply -auto-approve
terraform show
terraform state list
terraform output
terraform destroy
```
